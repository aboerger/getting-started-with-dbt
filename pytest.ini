[pytest]
# Unit tests for the bundle tooling (tools/) and the in-notebook runner (runner/).
# Run from .venv-tools:  python -m pytest
testpaths = tools/tests runner/tests
pythonpath = tools runner/src
addopts = -q
